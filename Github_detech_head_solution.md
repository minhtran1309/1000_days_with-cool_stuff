for some reasons, when you clone your project into the new desktop

`git checkout origin/<BRANCH-NAME>`
then try the following commands
```
git remote update
git fetch 
git checkout --track origin/<BRANCH-NAME>
```
